"$schema" = "schemas/schema.json"

[[permission]]
identifier = "secure-secrets"
description = "Allows the main window to store, load, and delete API keys in native secure storage."
commands.allow = [
  "writingway2_save_secret",
  "writingway2_load_secret",
  "writingway2_delete_secret",
]
